version: '3'

services:
  simple-spirit:
    container_name: simple-spirit
    hostname: simple-spirit
    restart: always

    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "8100:8100"
    depends_on:
      - mongo

  mongo:
    image: simple-mongo

